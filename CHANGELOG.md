# 开发日志

记录批量打印系统从原型到 v2.0 的完整迭代过程。

---

## v1.0 — 基础功能

**核心实现**
- 纯前端邮件合并功能（Excel → 模板 → 打印）
- SheetJS 解析 Excel，浏览器内完成，不上传服务器
- 画布拖拽排版 + 属性面板精确调整（mm 单位）
- 模板保存/加载（JSON 格式，含背景图 base64）
- 纸张选择：A4 / A5 / Letter / 自定义
- 打印输出用 `mm` 物理单位，避免 px 映射偏差
- `@page size` 动态匹配纸张尺寸

---

## v1.1 — 交互增强

**标尺系统**
- 画布四周增加毫米标尺（水平 + 垂直）
- 每 1mm 细刻度、5mm 次刻度、10mm 主刻度带数字标签
- 左上角灰色角标填补空白区域

**范围打印**
- 批量打印前弹窗选择起止行号
- 默认选中全部数据，可精确控制打印范围
- 起止行输入框联动校验（起始 ≤ 结束）

**工程文件**
- 区分"模板"（仅版式）和"工程"（版式 + Excel 数据）
- 工程文件完整自包含，复制到任何电脑打开即用
- 数据不依赖浏览器缓存

---

## v1.2 — 吸附与微调

**参考线与吸附**
- 拖拽时显示红色十字参考线（延伸到画布边缘）
- 软吸附设计：只提示对齐位置，不强制跳变，保持拖拽丝滑
- 对齐检测：左/右/水平中心、上/下/垂直中心
- 吸附阈值 2mm

**键盘微调**
- 方向键：0.1mm 精确微调
- Shift + 方向键：1mm 大步调整
- 位置下限为 0，防止负数坐标

---

## v2.0 — 字体与生产稳定性

### 字体系统重构

**系统字体扫描**
- 预置 300+ 常见字体候选列表（中文/英文/日文/韩文/等宽）
- 使用 `document.fonts.check()` 浏览器原生 API 检测
- 页面加载时自动扫描，只显示系统中实际存在的字体

**自定义字体**
- 支持手动添加系统中新安装的字体
- 防重复添加校验
- 字体列表实时刷新按钮

**字体选择器兼容性**
- 修复 Chrome datalist 有值时不弹列表的问题
- focus 时临时清空值，blur 时恢复（带状态标记防竞态）
- 支持 `showPicker()` API（部分浏览器）

### 排版增强

**颜色精确控制**
- 颜色选择器 + 十六进制文本输入联动
- 自动补全 `#` 前缀
- 实时校验 hex 格式

**垂直对齐**
- 新增垂直对齐：顶部 / 居中 / 底部
- 画布和打印输出均支持
- 打印时使用 `transform: translateY()` 实现

**背景图打印开关**
- 排版时背景图正常显示辅助对齐
- 工具栏新增"打印时包含背景图"复选框
- 取消勾选后只打印字段文字，不打印底图

### 生产稳定性修复

| 问题 | 级别 | 修复 |
|------|------|------|
| 打印 transform 残留（居中改回顶部后偏移） | P0 | 无对齐时显式重置 `transform: ''` |
| 打印后内存泄漏（100+ 页 DOM 节点不释放） | P0 | `afterprint` 事件自动清理 `printContainer` |
| `@page` 嵌套在 `@media` 内不兼容 | P0 | 提取到顶层独立声明 |
| 加载工程丢失自定义字体 | P1 | save/loadProject 中加入 `customFonts` |
| 打印分页截断字段 | P1 | 添加 `page-break-inside: avoid` |
| 大批量打印延迟不足 | P1 | 动态延迟：`Math.min(2000, 100 + count * 5)` |
| 输入框重复选择不触发 | P1 | `try...finally` 保证 `e.target.value = ''` |
| 字体选择器第二次点击失效 | P1 | `focus` 替代 `mousedown`，加 `isDatalistOpen` 状态 |

### 性能优化

- 大批量打印使用 `DocumentFragment` 批量插入，减少 DOM 重排
- 字体输入实时预览使用 30ms debounce
- 数据预览限制 50 行，避免超大表格卡顿

---

## 技术决策记录

| 决策 | 选择 | 原因 |
|------|------|------|
| 打印单位 | `mm` 物理单位 | `px` 在不同 DPI 屏幕和打印机之间映射不一致 |
| 字体检测 | `document.fonts.check()` | Canvas `measureText` 受隐私保护干扰，像素比对不可靠 |
| 数据存储 | JSON 文件导出 | 用户要求数据随文件迁移，不依赖浏览器缓存 |
| 分页控制 | `@page { size }` + `page-break-after` | CSS 打印标准，浏览器原生支持 |
| 背景图存储 | base64 嵌入 JSON | 保证模板/工程的自包含和可迁移性 |

---

## 已知限制

1. **字体 fallback**：打印目标电脑未安装指定字体时，浏览器会自动替换，可能导致排版偏移
2. **浏览器打印引擎差异**：Chrome/Safari/Firefox 的打印预览可能有亚像素级位置差异
3. **背景图大小**：base64 嵌入会导致工程文件体积膨胀，建议压缩图片
4. **大数据量**：Excel 行数超过 5000 时，数据预览和打印生成可能有明显延迟

---

## v2.1 — 交互优化（2026-06-13）

### P0 功能缺陷修复

| 问题 | 修复 |
|------|------|
| 右键点击画布会错误选中字段 | `onCanvasMouseDown` 增加 `e.button !== 0` 校验 |
| 拖拽中途按 ESC 取消，`.dragging` 样式残留 | 为 `fieldList` 增加 `dragend` 事件清理 |
| Excel 首行全空时 colCount=0 导致零字段 | 取 `Math.max(json[0].length, json[1]?.length \|\| 0)` |
| 背景图 FileReader 失败无提示 | 增加 `reader.onerror` 处理 |

### P1 体验断层修复

| 问题 | 修复 |
|------|------|
| 点击面板不取消字段选中 | 全局 `mousedown` 监听，点击非字段/非面板区域即取消 |
| 3位 Hex 颜色不支持（如 #fff） | 正则扩展为 `{3}({3})?`，并自动展开为6位 |
| 字段重叠时无法调整层级 | 属性面板新增「置顶」「置底」按钮，通过 DOM 顺序控制 |
| Excel 列数过多时数据预览溢出 | `.data-preview` `overflow-y: auto` 改为 `overflow: auto` |
| 操作提示未更新 | 新增层级、缩放、自动恢复的说明 |
| 模板文件不包含 zoom 比例 | `saveTemplate` / `loadTemplate` 加入 `zoom` |

### P2 细节优化

| 问题 | 修复 |
|------|------|
| 预览弹窗翻页无键盘支持 | `ArrowLeft` / `ArrowRight` 翻页；画布微调在弹窗打开时自动禁用 |
| 画布缩放后选中边框视觉变细 | `.canvas-field` border 使用 `calc(1px * var(--canvas-zoom))` 动态调整 |
| 代码内 `debouncedSave()` 缩进错误 | `setZoom` / `addCustomFont` 中补全缩进 |
| localStorage 满时静默失败 | `catch` 块改为 `console.warn` 提示用户手动导出 |

### 技术决策
- 选中边框缩放使用 CSS 自定义属性 `--canvas-zoom`，避免 JS 每帧计算
- 键盘事件全局统一监听，通过弹窗可见性做条件分流

---

### 工具栏布局优化
- 工具栏改为 `flex-wrap: nowrap` + 横向滚动，缩放控件不再换行跳动
- 各控件间距压缩，更紧凑

### localStorage 自动恢复
- 页面刷新/关闭前自动保存当前配置（模板 + Excel + 缩放 + 字体）
- 重新打开页面时自动恢复上次工作状态
- 数据存在浏览器本地，不依赖网络


- [ ] 支持图片字段（头像、二维码、条码）
- [ ] 支持多页模板（如双面打印）
- [ ] 支持条件打印（某字段为空时隐藏整行）
- [ ] 批量导出 PDF（不依赖浏览器打印对话框）
- [ ] 历史版本回退（工程文件多版本管理）
